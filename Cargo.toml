[build]
  command = "curl --proto '=https' --tlsv1.2 -sSf https://rustup.rs | sh -s -- -y && export PATH=$HOME/.cargo/bin:$PATH && cargo install dioxus-cli --version 0.5.6 && dx build --release"
  publish = "target/dx/begin/release/web/public"
